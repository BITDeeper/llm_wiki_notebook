---
title: "TPAMI 2026 | 基础架构优化，中科院等提出 EVT：为ViT引入欧几里得距离衰减，建模视觉空间先验"
date: 2026-05-03
source: "我爱计算机视觉"
url: "https://mp.weixin.qq.com/s/nhof3r5koKJJ0Tmdmm1ygw"
rss_id: "2247659310_1"
---
在计算机视觉领域，Vision Transformer（ViT）的出现无疑是一场变革。然而，尽管它在捕捉全局依赖方面表现出色，但相比于传统的卷积神经网络（CNN），它在某种程度上“缺乏常识”——也就是我们常说的空间先验（Spatial Priors）。此外，自注意力机制那随分辨率平方增长的计算复杂度，也让它在处理高分辨率图像时显得有些吃力。
为了解决这些问题，中国科学院与北京科技大学的研究团队在之前 RMT 模型的基础上，推出了一款增强版架构：
EVT
。该模型全称为
Euclidean enhanced Vision Transformer
，意为“欧几里得增强视觉 Transformer”。作者之所以起这个名字，核心在于它引入了更符合人类视觉特性的“欧几里得距离衰减”来提供空间先验，并配合一种极其灵活的 1D 标记分组策略，实现了性能与效率的双重飞跃。
论文地址
: https://arxiv.org/abs/2604.18549
录用情况
: TPAMI 2026
背景与动机：从“方格子”到“圆周率”
在探讨 EVT 之前，我们不得不提它的前作 RMT。RMT 曾尝试通过“曼哈顿距离（Manhattan distance）”来引入空间衰减，即让模型在计算注意力时，距离越远的标记权重越小。
但研究团队发现，曼哈顿距离的衰减模式在空间上呈现出一种“菱形”或“方块状”的分布，这与人类视觉系统的径向衰减（Radial Pattern）并不一致。想象一下，当你盯着屏幕中心时，你的注意力是向四周圆周式扩散的，而不是沿着直角坐标轴。
此外，RMT 采用的行列分解注意力机制虽然降低了复杂度，但其串行计算的特性限制了硬件的并行效率。为了打破这些瓶颈，EVT 带着两个核心改进登场了：
欧几里得距离衰减
：用更自然的径向距离取代直角距离。
1D 标记分组
：用更灵活的序列分组取代复杂的行列分解。
EVT 的整体架构图，展示了从 Conv Stem 到四个阶段的 EVT Block 堆叠过程
方法详解：欧几里得先验与 1D 分组的妙用
EVT 的整体架构延续了主流的分层设计，分为四个阶段（Stage），逐步降低特征图分辨率并增加通道数。其输入为
的图像，经过 Conv Stem 下采样后进入核心 Block。
1. 欧几里得自注意力（EuSA）
这是 EVT 的灵魂所在。在计算自注意力分数时，模型引入了一个基于欧几里得距离的衰减矩阵
。其数学表达：
其中，
是标记的物理坐标，
是一个控制衰减强度的超参数。通过将这个矩阵与原始注意力分数逐元素相乘（
），模型被显式地赋予了“近大远小”的空间直觉。
作者通过 JS 散度（Jensen-Shannon divergence）分析发现，这种欧几里得衰减矩阵生成的分布，与那些经过海量数据训练后的 ViT 模型（如 DINOv2）自发学习到的注意力分布高度相似。这意味着，我们不需要数亿张图片的预训练，就能让模型直接获得这种高质量的空间感知力。
不同距离矩阵与无衰减模型注意力分数的 JS 散度对比，欧几里得距离表现最接近自然分布
有趣的是，研究团队还测试了更复杂的距离函数，如 Minkowski 距离（
）或高斯 RBF 核。结果发现，这些高阶函数虽然理论上更平滑，但会导致训练极度不稳定，甚至出现
NaN
。相比之下，欧几里得距离在性能与稳定性之间取得了完美的平衡。
2. 1D 标记分组策略
为了处理高分辨率图像，EVT 放弃了 RMT 中那种复杂的水平/垂直分解，转而采用了一种看似简单却极其有效的
1D 分组方法
。
具体来说，模型将 2D 特征图展开成 1D 序列，然后直接进行分组（Grouped EuSA）或扩张分组（Dilated EuSA）。
Grouped EuSA (
)
：负责捕捉局部信息。
Dilated EuSA (
)
：通过跨步采样，负责捕捉全局依赖。
分组 EuSA 和扩张 EuSA 的示意图，展示了 1D 序列下的标记交互方式
你可能会担心：1D 分组不是破坏了 2D 空间结构吗？ 这正是 EVT 的巧妙之处。因为我们在 EuSA 中已经引入了基于 2D 物理坐标的欧几里得衰减，所以即便标记在序列中是“1D 邻居”，模型依然知道它们在物理空间上的真实距离。
更重要的是，作者从理论上证明了 1D 分组的
覆盖率（Coverage Ratio）
优势。在相同的计算预算下，1D 分组每层覆盖的标记数量是 2D 分组的
倍（
为组数）。这意味着 EVT 能以更快的速度获得全局感受野。
1D 分组与 2D 分组的对比，1D 方式在保持空间先验的同时提供了更长的交互范围
实验结果：全线飘红的性能指标
研究团队在
NVIDIA A100 GPU
上对 EVT 进行了全面的评测，涵盖了分类、检测、分割以及鲁棒性测试。
ImageNet-1K 分类任务
在 224x224 分辨率下，
EVT-L
达到了
85.8%
的 Top-1 准确率。当分辨率提升至 384x384 时，准确率进一步飙升至
86.6%
。更令人印象深刻的是，EVT-T（微型版本）仅凭 15M 的参数量就拿下了
83.0%
的成绩，显著优于同量级的 Swin-T 和 ConvNeXt-T。
EVT 各个变体在 ImageNet-1K 上的性能表现，展示了其在不同 FLOPs 下的竞争力
目标检测与分割
在 COCO 数据集上，EVT 同样展现了强大的下游迁移能力。使用 Mask R-CNN 框架时，
EVT-B
的
达到了
53.3
，比 RMT-B 提升了 1.1 点。
COCO 数据集上的检测结果对比，EVT 在各个指标上均领先于 RMT 和 Swin
在 ADE20K 语义分割任务中，EVT-L 配合 UperNet 框架刷出了
53.6%
的 mIoU，这一表现甚至超越了许多参数量大一个数量级的模型。
推理效率与稳定性
得益于 1D 分组策略对硬件的友好性，EVT 的推理速度非常快。在 A100 上，EVT-T 的吞吐量达到了
2142 imgs/s
，相比 RMT-T 的 1650 imgs/s 提升了近 30%。
此外，Fig 5 的 Loss 曲线清晰地展示了欧几里得距离在训练初期的稳定性优势。相比曼哈顿距离，欧几里得距离的收敛过程更加平滑，最终达到的精度也更高。
Loss 曲线对比，展示了欧几里得距离在训练稳定性上的卓越表现
不同模型的推理吞吐量对比，EVT 实现了更好的精度-速度平衡
写在最后
从 RMT 到 EVT 的进化，不仅仅是距离度量方式的改变（Manhattan
Euclidean），更是一次对视觉模型“底层逻辑”的重构。
从 RMT 到 EVT 的演进路线图，每一步改进都带来了实打实的性能提升
通过下图可视化对比，我们可以直观地看到：引入欧几里得先验后，模型的注意力分布变得更加集中且平滑，有效减少了注意力分散（Attention Dispersion）现象。这说明，符合物理直觉的先验知识，能够显著降低 Transformer 的学习难度。
注意力分布可视化，EVT 的注意力更加集中且符合物体轮廓
入群加好友(v:xiao-ma-baoli)，请备注你感兴趣的技术方向
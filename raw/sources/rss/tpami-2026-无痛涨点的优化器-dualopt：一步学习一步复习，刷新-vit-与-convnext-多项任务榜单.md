---
title: "TPAMI 2026 无痛涨点的优化器 DualOpt：一步学习一步复习，刷新 ViT 与 ConvNeXt 多项任务榜单"
date: 2026-05-04
source: "我爱计算机视觉"
url: "https://mp.weixin.qq.com/s/ZJOwio4aFUMRD3cax_S2Gg"
rss_id: "2247659311_1"
---
在深度学习的日常实践中，我们通常会面临两种选择：要么从零开始（From Scratch）训练一个模型，要么拿现成的预训练权重进行微调（Fine-tuning）。虽然这两种范式的目标不同，但大家往往都习惯性地套用相同的优化器，比如 Adam 或 SGD。然而，这种“一刀切”的做法真的最优吗？
近日，来自中国科学院、南洋理工大学、国防科技大学、中国科学技术大学等机构的研究者们在顶级期刊
IEEE TPAMI
上发表了一项最新研究，提出了一种名为
DualOpt
的新型优化框架，其核心思想是针对“从零训练”和“微调”这两种截然不同的任务需求，提供解耦的优化策略。它不仅能让模型在从零训练时收敛得更快、更稳，还能在微调时有效缓解“灾难性遗忘”问题。
论文地址
: https://arxiv.org/abs/2604.22838
主要机构
: 中国科学院、南洋理工大学、中国科学技术大学、蒙特利尔学习算法研究所（Mila – Quebec AI Institute）、国防科技大学
代码仓库
: https://github.com/qklee-lz/OLOR-AAAI-2024
录用信息
: TPAMI 2026
背景与动机：为什么优化器需要“解耦”？
传统的优化器如随机梯度下降（Stochastic Gradient Descent, SGD）和自适应矩估计（Adaptive Moment Estimation, Adam），其设计初衷主要是为了最小化损失函数。但在实际应用中，从零训练和微调的“痛点”完全不同：
从零训练
：参数随机初始化，重点在于如何快速收敛并获得良好的泛化能力。现有的权重衰减（Weight Decay）机制虽然能防止过拟合，但作者在理论上证明了，在某些更新步中，它可能会意外地增加权重幅度，反而削弱了正则化效果。
微调
：模型已经具备了上游任务的通用知识。此时，优化器的挑战在于如何在学习新任务的同时，不把老知识丢掉（即缓解知识遗忘）。
DualOpt 的出现，正是为了打破这种统一优化的局限性，通过一套框架、两套逻辑，精准解决上述问题。
DualOpt 框架总览。它集成了用于微调的权重回退机制和用于从零训练的逐层权重衰减。
方法详解：因地制宜的优化策略
DualOpt 将优化路径分为两条：针对从零训练的“实时逐层权重衰减”和针对微调的“权重回退与逐层惩罚”。
1. 从零训练：实时逐层权重衰减（Real-Time Layer-Wise Weight Decay）
作者首先指出，传统的权重衰减往往滞后于参数更新。标准的更新公式为
。作者通过数学推导发现，当满足以下条件时：
权重的绝对值反而会增加，这违背了正则化的初衷。为了解决这个问题，DualOpt 引入了
实时权重衰减
，直接对更新后的临时权重进行衰减：
这样可以确保在任何
的情况下，权重都会被有效地向零拉拢。
更巧妙的是，它考虑到了神经网络的层级特性。浅层网络通常捕捉颜色、边缘等基础特征，过拟合风险低；而深层网络捕捉复杂的语义特征，更容易过拟合。因此，DualOpt 采用了
逐层权重衰减率
：
其中
是当前层数，
是总层数。这意味着越往深层，衰减力度越大，从而在保证收敛速度的同时，提升了模型的泛化性能。
DualOpt 优化框架流程图。顶部路径处理从零训练，底部路径处理微调。
2. 微调：权重回退机制（Weight Rollback）
这是 DualOpt 最具创新的部分。在微调过程中，它在每次更新步中加入了一个“回退项”，将当前的权重稍微向预训练权重
方向“拉”一把。
输入输出流程
：
输入
：当前权重
、梯度
、预训练权重
、差异值
。
逻辑
：优化器首先计算梯度更新，然后计算当前权重与
的偏差。
更新公式
：
。
输出
：更新后的权重
以及更新后的差异值
。
这种设计就像是“一步学习，一步复习”，确保模型在适应新任务的同时，其权重分布不会偏离预训练状态太远。
此外，针对微调，DualOpt 还设计了
逐层惩罚（Layer-wise Penalty）
。由于浅层特征更具通用性，DualOpt 会通过参数
调节惩罚力度，给浅层施加更强的回退惩罚，以保留通用知识；而深层则给予更多自由度去适配新任务。
微调模式下的 Adam 优化器算法流程，展示了权重回退与差异更新的细节。
实验与结果：全方位的性能碾压
研究团队在图像分类、目标检测、语义分割等 10 个主流数据集上进行了严苛的测试，涵盖了 ResNet、Inception、ConvNeXt 和 ViT 等多种主流骨干网络。
1. 分类任务：全面超越 SOTA
在微调实验中，DualOpt 展现了惊人的爆发力。以 ViT-B 为例，在挑战性极高的跨域数据集 PACS 上，DualOpt 将准确率从基线的 87.79% 提升到了
94.38%
。在 CIFAR-100 上，它也比传统的 Full Fine-tuning 提升了约 5.13 个百分点。
微调任务结果对比。可以看到 DualOpt 在各种数据集上均取得了最优表现。
在大规模数据集 ImageNet 上，DualOpt 同样表现出色。对于 ViT-B 模型，它将 Top-1 准确率从 81.34% 提升至
82.58%
；对于 Swin-B 模型，则从 83.05% 提升至
83.89%
。
2. 检测与分割：通用性极强
DualOpt 并不局限于分类。在 COCO2017 目标检测任务中，使用 Mask R-CNN 作为基准，DualOpt 将 Bbox mAP 从 40.20% 提升至
41.10%
。
在 ADE20K 语义分割任务中，使用 UperNet 配合 ViT-B 后，mIoU 也提升了近 1 个百分点，达到
44.62%
。
3. 效率分析：极低的额外开销
作为一个优化器层面的改进，大家最关心的往往是计算成本。实验显示，DualOpt 相比于全量微调，在 ViT-B 上仅增加了约
0.04%
的计算开销（FLOPs），显存占用（Mem）也几乎持平。这意味着你几乎可以“无痛获得”这些性能提升。
方法效率对比表。DualOpt 在保持高性能的同时，计算开销极小。
4. 可视化分析：特征边界更清晰
通过 t-SNE 可视化可以发现，DualOpt 学习到的特征分布具有更小的类内距离和更大的类间距离。相比于 Linear Probing 或 Full Fine-tuning，DualOpt 的特征簇边界非常清晰，这直接解释了其高准确率的原因。
PACS 数据集上的 t-SNE 可视化。DualOpt 的特征分离度明显优于其他方法。
写在最后
DualOpt 的成功告诉我们，深度学习的进步不仅来自于更深的网络或更大的数据，对底层优化机制的“精雕细琢”同样能带来显著的红利。它通过简单的数学推导和对网络结构的深刻理解，进一步缓解了训练范式中的不匹配问题。
这种
“一步学习，一步复习”
的逻辑，不仅在视觉任务中大放异彩，其背后的解耦思想对于大语言模型（LLM）的持续学习、指令微调等领域同样具有继续探索的启发意义。
---
title: "清华&淘宝直播开源CoInteract：双流协同生成，实现物理一致的人物交互"
date: 2026-04-29
source: "我爱计算机视觉"
url: "https://mp.weixin.qq.com/s/eJdBQ-3fzSH_ETVPFNdmgw"
rss_id: "2247659257_1"
---
在视频生成技术突飞猛进的今天，我们已经能看到非常逼真的数字人说话视频。但当场景从“坐着说话”变成“拿起产品演示”时，AI 往往就开始露怯了：手部可能会穿过包包的表面，或者手指在抓握时变得模糊不清。这种人与物体交互（Human-Object Interaction, HOI）的物理不一致性，一直是视频生成领域的顽疾。
近日，来自清华大学和阿里巴巴淘宝直播的研究团队提出了一种名为
CoInteract
的端到端框架，结合了
Co
-Generation（协同生成）与
Interact
ion（交互）两个概念，旨在通过空间结构协同生成的范式，让 AI 视频不仅好看，更要符合物理规律。
论文地址
: https://arxiv.org/abs/2604.19636
项目主页
: https://xinxiaozhe12345.github.io/CoInteract_Project
代码仓库
: https://github.com/luoxyhappy/cointeract
值得一提的是，作者已经在有序推进开源，相关论文在 Huggingface Daily Paper取得了日榜第二，周榜第四的排名，可见业界对其的关注热度。
为什么 AI 总是学不会“拿东西”？
在电商直播领域，合成人物与物体交互（Human-Object Interaction, HOI）应用——允许用户仅提供一张人物参考图、一张商品参考图和文本描述，即可生成逼真的商品展示视频，而无需实际拍摄。
而目前的人-物交互视频生成难以满足这样的需求。
目前的视频扩散模型大多是“以像素为中心”的。模型在训练时只学习怎么看起来更像照片，但它脑子里并没有“3D 空间”和“物理边界”的概念。这就导致了两个典型失败案例：一是
结构崩溃
，比如手指抓握时融合在一起；二是
物理违和
，比如手直接穿透了物体表面（即“穿模”）。
现有的方法要么依赖沉重的推理时预处理（如提取每一帧的精确姿态），要么缺乏对交互结构的显式约束。CoInteract 的出现，就是想把“结构先验”和“物理约束”直接植入生成模型的大脑——Diffusion Transformer (DiT) 的骨干网络里。
CoInteract 与现有生成范式的对比，展示了其端到端、易用且感知结构的优势
方法详解：给 DiT 装上“物理外挂”
CoInteract 主要由两个互补的设计组成：
空间结构协同生成(Spatially-Structured Co-Generation)
和
人体感知专家混合模块（Human-Aware Mixture-of-Experts, MoE）
。
1. 双流协同生成：让 AI 边画图边想结构
研究团队引入了一个辅助的“HOI 结构流”。简单来说，模型在训练时不仅要生成彩色的 RGB 视频，还要同步生成一个只有剪影和物体掩码的结构图。
Input（输入）
：包括人物身份参考图
、产品参考图
、文本提示词、语音音频，以及为了保证连贯性的历史运动帧
。
Output（输出）
：高保真的 RGB 交互视频。在训练阶段，还会输出对应的 HOI 结构流视频。
为了让这个过程更高效，作者设计了
非对称协同注意力（Asymmetric Co-Attention）
机制。在训练的第一阶段，两个流完全互通，建立耦合；到了第二阶段，通过一个特殊的掩码
，让 RGB 流的查询（Query）只关注自己，而 HOI 流则可以观察 RGB 流。
两阶段训练与推理示意图，展示了如何通过掩码实现推理加速
掩码的逻辑如下：
其中
和
分别代表 RGB 和 HOI 的 Token 集合。这样做的好处在于：
在推理阶段，我们可以直接扔掉 HOI 分支，实现零额外开销的生成，但此时的 RGB 骨干网络已经通过共享参数“吸收”了结构流带来的物理约束。
2. Human-Aware MoE：给手和脸开个“小灶”
手部和脸部是人类视觉最敏感的区域，也是最容易出 Bug 的地方，需要被特别关照。CoInteract 在 DiT 块中嵌入了人体感知 MoE。
空间监督路由
：模型利用人脸和手部的边界框（Bbox）作为监督，训练一个轻量级路由器。为了不干扰主干特征学习，路由器输入前会进行梯度截断（Stop-gradient）。
专项专家
：路由器会将属于手部的 Token 发送给“手部专家”（Hand Expert），将脸部发送给“头部专家”（Head Expert）。这些专家是专门针对这些高频细节优化的轻量级全连接网络（FFN）。
CoInteract 整体框架图，展示了双流生成和 MoE 路由的细节
3. 3D RoPE：统一的时空坐标系
为了处理历史运动、参考图、RGB 流和结构流等多种模态，模型使用了
3D 旋转位置编码（3D Rotary Positional Encoding, 3D RoPE）
。它给每个 Token 分配了独特的
坐标：
空间维度
：RGB 和 HOI 流在宽度维度上进行偏移拼接，共享高度坐标。
时间维度
：历史运动帧被分配负的时间索引（如
），而静态参考图则被映射到远场时间位置（如
），确保模型能分清“因果关系”和“全局身份锚点”。
4. 数据预处理：构建“纹理剥离”的结构流
为了训练这个模型，研究团队开发了一套精密的预处理流水线。如下图所示，他们首先利用 Qwen-Edit 对视频进行实体解耦，提取独立的个人和产品参考图。接着，利用 SAM3 获取物体掩码，并结合 SAM3D-body 恢复人体网格（Mesh），将其投影到图像平面。
预处理流水线，用于构建配对的 RGB 和 HOI 结构训练数据
这种“纹理剥离”的 HOI 结构流迫使模型学习纯粹的几何交互，而不是通过颜色纹理来走捷径。
实验结果：物理一致性的新标杆
研究团队构建了一个包含 40 小时产品演示视频的数据库，并清洗出 12K 个高质量片段进行训练。在定量评估中，CoInteract 在多个维度刷新了记录。
特别值得关注的是
VLM-QA
指标（利用 Gemini-3-Pro 进行的交互合理性问答）和
HQ
（手部质量，基于 DWPose 的置信度得分）。
交互合理性
：CoInteract 达到了
0.72
，远超次优模型 InteractAvatar 的 0.62。
手部质量
：得分
0.724
，证明了 MoE 模块在稳定手部结构方面的巨大价值。
定量对比表，CoInteract 在交互质量和参考一致性上表现卓越
从定性对比图（下图 5）可以看到，其他模型在处理“调整包带”或“拿着毛绒玩具”等动作时，经常会出现手部变形或物体扭曲，而 CoInteract 生成的视频在手部抓握细节和物体稳定性上明显更胜一筹。
定性对比，CoInteract 在复杂交互场景下的表现更加稳健
在用户研究中，CoInteract 在物体一致性、人/背景一致性以及最重要的交互合理性上，均获得了最低的平均排名（Mean Rank，越低越好），这进一步证实了其在真实感上的领先地位。
用户研究结果，CoInteract 在多项主观指标上均排名第一
写在最后
CoInteract 说明了
对于复杂的物理交互任务，单纯堆砌像素层面的数据可能还不够，将结构先验和几何约束内化到模型架构中，往往能起到事半功倍的效果。
这种“训练时有结构引导，推理时无额外开销”的设计思路，非常适合落地到对实时性和质量都有高要求的场景，比如电商直播、虚拟试穿等。
目前该项目推理代码已开源，作者称将会在近期完整开源训练和姿态控制代码，感兴趣的小伙伴可以期待一下。
入群加好友(v:xiao-ma-baoli)，请备注你感兴趣的技术方向
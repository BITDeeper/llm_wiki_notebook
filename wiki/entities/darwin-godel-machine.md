---
type: entity
title: Darwin Gödel Machine (DGM)
tags: [meta, agent, self-improvement, framework, history, algorithms, self-modification, open-endedness]
related: [hyperagents, jenny-zhang, 自进化架构, 哥德尔机, jurgen-schmidhuber, 开放式算法]
created: 2026-04-06
updated: 2026-05-08
sources: ["ai学会左脚踩右脚自进化？meta华人新研究改写agent法则.md", "meta华人实习生搞出超级智能体！自己写代码实现自我进化.md"]
---

# Darwin Gödel Machine (DGM)

**Darwin Gödel Machine** (简称 DGM，达尔文哥德尔机) 是 [[hyperagents]] 的前身与基石架构，由 [[Meta]] 团队的 [[Jenny Zhang]] 等人提出。该框架旨在通过递归自我改进来提升 AI 智能体的性能，其核心思想结合了 [[Jurgen Schmidhuber]] 提出的哥德尔机（Gödel Machine）概念与达尔文进化论。

## 设计原理

### 解决“净收益证明”难题
传统的哥德尔机理论要求 AI 在修改自身代码前，必须从数学上证明该改动的“净收益”（即性能提升带来的收益必须大于计算成本）。这一要求在现实世界的复杂任务中几乎无法实现。

DGM 通过以下机制绕过了这一数学证明的瓶颈：
1. **利用基础模型提议改进**：使用 LLM 生成代码修改方案。
2. **引入开放式算法**：利用 [[开放式算法]] 在大量提议中搜索，构建一个不断增长的、多样化且高质量的 AI 智能体库。
3. **经验性筛选**：通过实际测试和验证（而非数学证明）来筛选有效的改进方案。

### 工作流程
DGM 通过以下循环机制实现自我进化：
1. **自改代码**：Agent 修改自己的源代码以修复错误或增加功能。
2. **自我验证**：验证修改后的版本是否有效。
3. **存档**：将成功的版本存入 Archive 作为“垫脚石”。
4. **迭代**：基于历史成功版本继续进化。

### 自我改进实例
在实验中，DGM 能够自主发现并实施多种具体的自我改进策略，例如：
- 增加补丁验证步骤。
- 优化文件查看功能。
- 增强编辑工具。
- 自动添加历史尝试记录并分析失败原因。

## 局限性

DGM 的有效性高度依赖于**任务能力与自修改能力的对齐**。

- **适用场景（对齐）**：在编程任务中表现优异。因为“写代码”和“改代码”的能力是对齐的，模型在任务上的提升直接转化为改进机制的能力。
- **失效场景（不对齐）**：在非编程任务（如论文评审、数学评分）中，任务能力的提升并不直接转化为改进机制的能力，导致 DGM 几乎失效，除非进行人工定制（DGM-custom）。

这种局限性被称为系统的**自指性** 瓶颈。

## 与 HyperAgents 的关系

DGM 的 Meta 层（即决定如何修改的机制）是手工设计且固定的，这成为了系统进一步进化的瓶颈。为了解决这一问题，Meta 团队开发了继任者 [[hyperagents]] (DGM-H)。

Hyperagents 将 Meta 层也纳入了可修改的范围，从而实现了跨域的自我进化，克服了 DGM 仅在特定领域有效的局限。

## 性能数据

DGM 在编程基准测试中展现了显著的性能提升：
- **SWE-bench**: 20.0% → 50.0%
- **Polyglot**: 14.2% → 30.7%
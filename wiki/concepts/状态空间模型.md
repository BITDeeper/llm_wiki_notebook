---
type: concept
title: 状态空间模型
tags: [algorithm, math, deep-learning, architecture, sequence-modeling]
related: [mamba-模型, 后-transformer-架构, lstm, gpt-5.4, 永久记忆, transformer]
created: 2026-03-16
updated: 2026-05-06
sources: ["奥特曼宣判transformer死刑！-agi两年内降临，下一代架构已在路上.md", "刚刚，gpt-5.4核心内幕炸裂剧透！或拥有永久记忆，极限推理狂飙.md"]
---

# 状态空间模型

**状态空间模型** (State Space Models, SSM) 是一类通过隐状态随时间演化来建模序列数据的数学框架。在深度学习领域，SSM 因 [[mamba-模型]] 的成功而成为 [[transformer]] 架构的重要替代方案，其核心思想是通过隐状态随时间演化的微分方程来建模序列数据。

## 工作原理

SSM 将序列建模视为一个连续系统下的状态演化问题。它通过一个固定的记忆状态来整合历史信息，而不是像 Transformer 那样存储并计算所有历史位置的键值对。

## 核心优势

SSM 相比传统 Transformer 架构具有以下显著优势：

- **计算效率 (线性复杂度)**：SSM 的推理和训练通常具有线性复杂度 $O(N)$，远优于 Transformer 的 $O(N^2)$。这使得 SSM 在处理百万级甚至更长上下文时极具效率。
- **长序列处理**：由于状态大小固定，SSM 在处理极长序列时不会面临显存不足的问题，理论上支持无限长度的上下文。
- **因果性**：SSM 天然具有因果性（当前输出仅依赖过去输入），非常适合自回归生成任务。
- **状态传递**：SSM 的设计天然支持在每一步计算中持续传递隐藏状态，这为实现 [[永久记忆]] 或持久状态提供了理论上的架构基础。

## 历史背景

SSM 并非全新概念，它在控制论和信号处理领域有悠久历史。在深度学习时代，LSTM（长短期记忆网络）和 GRU（门控循环单元）均可视为 SSM 的一种离散化形式。Mamba 等现代工作通过引入可参数化的机制和并行化算法，解决了传统循环网络训练慢的痛点。

## 在 GPT-5. 中的应用

在关于 [[GPT-5.4]] 的报道中，业界猜测 OpenAI 可能找到了大规模结合 SSM 和 Transformer 的方法。这种混合架构可能旨在：

1.  **突破上下文限制**：利用 SSM 的线性复杂度支持 100万甚至 200万 tokens 的上下文窗口。
2.  **实现持久状态**：利用 SSM 的状态传递机制，赋予模型跨会话的长期记忆能力。

## 相关条目
- [[mamba-模型]]
- [[后-transformer-架构]]
- [[lstm]]
- [[transformer]]
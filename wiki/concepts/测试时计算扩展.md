---
type: concept
title: 测试时计算扩展
tags: [ai, scaling-law, inference, optimization]
related: [muse-spark, 思维压缩, 多-agent-并行思考]
created: 2026-04-09
updated: 2026-04-09
sources: ["刚刚，小扎砸143亿的「牛油果」来了！硬刚gpt-5.4，硅谷最贵华人首作.md"]
---

# 测试时计算扩展

[[测试时计算扩展]]（Test-time Compute Scaling）是指在推理阶段通过增加计算量（如让模型「想得更久」）来提升模型性能的技术范式。这是当前 AI 领域提升模型推理能力的关键技术趋势之一。

## 核心挑战
在推理阶段增加计算量会带来两个主要问题：
1. **延迟增加**：用户等待时间变长。
2. **成本高昂**：Token 消耗量线性增长。

## 解决方案
[[Muse Spark]] 通过两种技术手段解决了上述挑战：

### 1. 思维压缩
通过在强化学习训练中引入「思考时间惩罚」，迫使模型学会 [[思维压缩]]。模型学会了用更少的 Token 解出同样的问题，从而降低了推理成本。数据显示，Muse Spark 的 Token 消耗仅为同类模型的 1/3。

### 2. 多 Agent 并行
采用 [[多 Agent 并行思考]] 机制，通过增加并行度而非延长单个 Agent 的思考时间来提升质量。这使得模型能够在不增加延迟的情况下提升准确率。

## 扩展曲线
Muse Spark 的训练过程展示了一个有趣的「相变」现象：
1. **膨胀期**：模型通过想更久来提升正确率。
2. **压缩期**：思考时间惩罚触发思维压缩，模型学会用更少 Token 解题。
3. **再膨胀期**：压缩完成后，模型再次拉长解题过程以挑战更难的题目。
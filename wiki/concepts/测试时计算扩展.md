---
type: concept
title: 测试时计算扩展
tags: ["ai", "scaling-law", "inference", "optimization", "推理优化", "计算扩展", "测试时", "llm", "推理", "计算范式", "推理计算", "模型能力", "技术趋势"]
related: ["muse-spark", "思维压缩", "多-agent-并行思考", "原生并行推理", "链式思维", "native-parallel-reasoner", "co-scientist", "scaling-law", "ai-for-science", "推理预算", "性能-推理计算量曲线", "noam-brown", "openai"]
created: 2026-04-09
updated: 2026-06-09
sources: ["刚刚，小扎砸143亿的「牛油果」来了！硬刚gpt-5.4，硅谷最贵华人首作.md", "icml-2026｜告别「单线程」思维，智能体进化出了原生的并行推理大脑.md", "谷歌gemini杀入科学界！一日两登nature，alphafold只是开胃菜.md", "rss/openai科学家noam-brown：ai的真正上限，可能根本没人测得起.md"]
origin_date: 2024-09-01
---

# 测试时计算扩展

[[测试时计算扩展]]（Test-Time Compute Scaling），又称"推理时计算扩展"（Inference-time Compute Scaling），是指在推理阶段通过分配更多计算资源（如让模型「想得更久」、多次采样、更长推理链、多路径探索、更多工具调用）来提升模型性能的技术范式。由 Snell et al. 在 ICLR 2025 提出，核心发现是优化测试时计算可能比扩大模型参数更有效。自2024年9月 OpenAI 发布 o1 系列推理模型以来，逐渐成为大模型发展的重要方向，是当前 AI 领域提升模型推理能力的关键技术趋势之一。

## 核心思想

传统方式通过增大模型参数提升性能，而测试时计算扩展探索在推理时投入更多计算来换取更高的准确率。与传统的训练阶段扩展（增加参数量、训练数据量）不同，测试时计算扩展关注的是模型在推理/部署阶段的计算资源投入与输出质量之间的关系。模型能够利用越来越长的运行时间和越来越大的推理预算，持续探索、试错和修正策略。

## 与 Scaling Law 的关系

测试时计算扩展是 [[scaling-law]] 的新应用维度，可视为 Scaling Law 在推理阶段的新维度延伸。传统 Scaling Law 的三要素（参数量、数据量、训练算力）之外，推理算力正成为第四个关键变量。此前 [[dario-amodei|Dario Amodei]] 曾预测2026年将迎来AI激进加速，测试时计算扩展为这一预测提供了具体的工程化路径：不仅模型训练需要扩展，推理阶段的计算投入同样可以成为性能提升的驱动力。

## 核心发现

- 性能平台期可能远比预期更晚出现：[[andrej-karpathy|Karpathy]] 的自动化研究实验中数百次试验后曲线仍未平缓
- 英国AI安全研究所评测中，部分模型累计使用超1亿 token 后表现仍持续提高
- 更强模型不仅起点更高，还更擅长将额外计算资源转化为有效能力

## 核心挑战

在推理阶段增加计算量会带来两个主要问题：

1. **延迟增加**：用户等待时间变长。
2. **成本高昂**：Token 消耗量线性增长。

## 解决方案

[[Muse Spark]] 通过两种技术手段解决了上述挑战：

### 1. 思维压缩

通过在强化学习训练中引入「思考时间惩罚」，迫使模型学会 [[思维压缩]]。模型学会了用更少的 Token 解出同样的问题，从而降低了推理成本。数据显示，Muse Spark 的 Token 消耗仅为同类模型的 1/3。

### 2. 多 Agent 并行

采用 [[多-agent-并行思考]] 机制，通过增加并行度而非延长单个 Agent 的思考时间来提升质量。这使得模型能够在不增加延迟的情况下提升准确率。

## 与并行推理的关系

[[原生并行推理]] 被视为扩展测试时计算能力边界的关键手段。[[native-parallel-reasoner|NPR]] 的实验表明，并行推理的加速比随任务难度增加而增大（AIME25 达 4.6 倍），证明在需要更深入探索解路径的困难问题上，并行推理的测试时计算扩展优势更加显著。

## 扩展曲线

Muse Spark 的训练过程展示了一个有趣的「相变」现象：

1. **膨胀期**：模型通过想更久来提升正确率。
2. **压缩期**：思考时间惩罚触发思维压缩，模型学会用更少 Token 解题。
3. **再膨胀期**：压缩完成后，模型再次拉长解题过程以挑战更难的题目。

## 行业现状

[[noam-brown|Noam Brown]] 指出，在该趋势出现近两年后，许多前沿模型发布仍然主要依靠单一基准分数进行传播和比较。他建议行业从"单点成绩"转向"[[性能-推理计算量曲线]]"，并将[[推理预算]]视为评测报告中的核心参数。

## 在 Co-Scientist 中的应用

[[co-scientist]] 是该范式的典型应用案例。其多智能体「点子锦标赛」机制中，随测试时计算量的增加，假设质量持续提升——投入更多推理计算资源，系统就能生成更高质量、更精细的科研假设。

## 意义

这一范式表明，AI系统的能力上限不仅取决于训练时的规模，还可以通过推理时的计算投入来持续突破，为 [[ai-for-science]] 等需要高质量输出的场景提供了新的优化方向。

## 参考文献

- Snell et al. "Scaling LLM Test-Time Compute Optimally Can be More Effective than Scaling Parameters for Reasoning." ICLR 2025.
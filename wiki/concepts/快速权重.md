---
type: concept
title: 快速权重
tags: [mechanism, optimization, llm]
related: [in-place-ttt, 测试时训练-ttt, mlp-多层感知机]
created: 2026-04-10
updated: 2026-04-10
sources: ["大模型能“原地”改参数了！字节seed&北大新论文：测试时推理无需加层重训练.md"]
---

# 快速权重

在神经网络（特别是 [[测试时训练-ttt]] 语境下）指代在推理过程中动态更新的参数，区别于训练阶段固定的“慢速权重”。

## 定义与作用
*   **定义**：模型在处理当前序列时，为了记住上下文信息而临时修改的参数。
*   **作用**：充当模型的“短期记忆”或“临时小脑”，使模型能够根据当前输入快速调整其行为，而无需改变底层的预训练权重。

## In-Place TTT 中的实现
在 [[in-place-ttt]] 方法中，快速权重的实现具有特殊性：
*   **载体**：直接复用 Transformer 中 [[mlp-多层感知机]] 的投影矩阵 $W_{down}$。
*   **优势**：不需要引入额外的内存或网络层来存储这些权重，实现了“原地”更新，降低了计算和存储开销。
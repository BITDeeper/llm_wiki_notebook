---
type: concept
title: 存算一体
tags: [计算机体系结构, 芯片设计, AI硬件]
related: [晶圆级集成, ouroboros, 数据搬运, 冯诺依曼瓶颈]
created: 2026-04-20
updated: 2026-04-20
sources: ["晶圆级芯片和存算一体结合：中科院提出15万tokenss晶圆级芯片方案丨asplos'26.md"]
---

# 存算一体

**存算一体** (Compute-in-Memory, CIM) 是一种旨在突破传统计算机架构中“冯·诺依曼瓶颈”的新型架构范式。

## 核心原理
在传统架构中，数据需要在存储单元（如内存）和计算单元（如 CPU/GPU）之间频繁搬运，这一过程被称为 [[数据搬运]]。存算一体技术通过将计算逻辑直接嵌入存储阵列中，或者让存储单元具备计算能力，使得数据可以直接在存储位置被处理，从而消除或大幅减少数据搬运的开销。

## 优势
- **降低延迟**：消除了数据在存储器和处理器之间往返的时间。
- **提高能效**：数据搬运是能耗的主要来源，消除搬运可显著提升能效比。
- **高带宽利用**：利用存储阵列内部的并行性，实现极高的计算吞吐量。

## 实现形式
- **模拟存算一体**：利用存储单元的物理特性（如电阻、电流）进行模拟计算（常用于神经网络加速）。
- **数字存算一体**：在存储阵列附近或内部嵌入数字逻辑单元，进行精确的数字计算（如 [[Ouroboros]] 采用的 SRAM 存算单元）。

## 与大模型推理的结合
对于大语言模型（LLM）而言，[[数据搬运]]（特别是权重和 KV Cache 的搬运）是推理延迟和能耗的主要瓶颈。[[Ouroboros]] 系统正是利用 [[晶圆级集成]] 提供的巨大片上 SRAM，结合存算一体技术，实现了所有数据的“原地计算”，从而达到了 15 万 tokens/s 的峰值性能。
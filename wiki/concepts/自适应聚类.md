---
type: concept
title: 自适应聚类
created: 2026-06-08
updated: 2026-06-08
tags: [稀疏注意力, 聚类, 效率优化, 序列压缩]
related: [rtpturbo-v2, 低秩投影压缩, 内生稀疏性]
sources: ["rss/阿里rtpurbov2：原生transformer再次崛起，百步训练实现10倍稀疏注意.md"]
---
# 自适应聚类

[[rtpturbo-v2|RTPurboV2]] 的核心技术之一，在序列维度将 N 个 token 聚类为 K 个语义簇，实现两阶段漏斗式计算。

## 动机

[[低秩投影压缩]]过滤高频噪声后，语义相似的 token 在低秩空间中天然聚拢，语义无关的 token 彼此远离，为序列维度的进一步压缩创造了理想条件。

## 计算流程

1. **粗粒度匹配**：将 N 个 token 聚类为 K 个语义簇（如 K=128），Query 先与 K 个簇中心做轻量级匹配，复杂度仅 O(N·K)
2. **细粒度计算**：仅在命中的相关簇内执行完整 Attention 计算

两阶段串联，整体复杂度从 O(N²) 降至 O(N·K)。

## 协同效应

与低秩投影形成乘法效应：
- 特征维度压缩 → 降低单步计算开销，同时产出高质量聚类输入
- 序列维度压缩（聚类）→ 跳过大量语义无关 token，降低总计算步数
- 协同效应 → 特征压缩提纯后的向量让聚类中心更精准，极端压缩比下依然保持高召回率

压缩比越激进，协同增益越显著。
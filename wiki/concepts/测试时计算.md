---
type: concept
title: 测试时计算
tags: ["推理", "优化", "算力", "ai", "概念", "性能", "具身智能", "机器人"]
related: ["scaling-law", "andy-jones", "openai", "claude-opus-4.5", "ai-resistant-technical-evaluations", "tau0-wm", "具身世界模型"]
created: 2026-02-23
updated: 2026-05-31
sources: ["openai大佬爆料：本科生靠一篇博客杀进openai！没博士，0篇论文.md", "刚刚，anthropic内部考题开源！年薪百万工程师，被ai秒了.md", "τ0-wm：最大规模预训练的开源具身世界模型来了.md"]
---

# 测试时计算

测试时计算（Test-Time Computation）是指在模型推理（测试/使用）阶段增加计算量，以提升模型性能或准确率的技术范式。它与预训练计算相对，是当前 AI 领域（如 OpenAI o1）的重要研究方向。

在实际应用中，这通常表现为模型在给出最终答案前进行的"思考"、"规划"或内部模拟过程——而非立即输出最终结果。

## 核心权衡

AI 研究者通常面临两种选择：

1. **扩大预训练规模**：增加训练时的算力和数据量（提升基础模型能力）。
2. **扩大测试时计算**：在推理时进行更多思考、搜索或验证（提升输出质量）。

## 在 LLM 领域的应用

在 LLM 领域，测试时计算通常指让模型生成更长的思维链或进行多次采样取最优。

### Anthropic 考题中的体现

在 [[Claude Opus 4.5]] 参与 [[original-performance-takehome]] 测试时，Anthropic 观察到了明显的测试时计算效应：

- 随着允许的"思考时间"增加，模型生成的代码优化程度显著提高（执行周期数从 1790 降至 1363）。
- 这表明 AI 在解决复杂工程问题时，并非简单的"查表"或"模式匹配"，而是通过扩展推理链路在进行真正的探索和优化。

### 研究案例

量化分析师 [[andy-jones]] 曾通过深度博客分析这两者的权衡影响。他自建 GPU 加速环境，进行了严谨的消融实验，展示了在特定场景下增加测试时计算的价值。这项工作帮助他成功入职 Anthropic。

### 典型应用场景

- **复杂推理任务**：如数学证明、代码生成，需要模型"多想几步"。
- **搜索与验证**：在生成答案前尝试多种路径并筛选最优解。

## 在具身智能中的应用

在具身智能中，测试时计算的核心差异在于需要与物理世界交互——模型不仅要"想"，还要在内部"虚拟沙盘"中"看"到动作的物理后果。

### τ0-WM 中的三步 Pipeline

在 [[τ0-WM]] 中，测试时计算体现为：

1. **提议**：采样多组候选动作，生成模糊未来画面。
2. **推演**：针对每组候选动作生成多视角未来画面。
3. **评估与修正**：通过 RCS 打分和 LAR 修正选出最优动作。

这使得机器人从"看到画面就立刻出手"转变为"先在脑子里盘一遍哪种路线更靠谱，再决定怎么做"——类似于让机器人学会一种"慢思考"。

### 实验验证

在 τ0-WM 的实验中，测试时计算的效果显著：

- 裸策略成功率：43%
- 加入 RCS 动作筛选：50%
- 叠加 LAR 模拟器修正：60%
- 对比方法 CFG：20%，ACG：38%

关键区别在于：CFG 和 ACG 检查的是"动作本身是否连贯"（动作空间内部一致性），而 τ0-WM 评估的是"动作做完之后，未来世界会变成什么样，任务有没有真的往前推进"（未来后果评估）。

## 意义与挑战

测试时计算是 [[Scaling Law]] 在推理阶段的延伸。它意味着 AI 的性能不仅取决于模型大小，还取决于在推理时愿意投入多少算力。

这也给"抗 AI 评估"带来了更大的挑战：如果 AI 只要"多想一会儿"就能解出题目，那么基于时间限制的考试将越来越难以区分人类和 AI。

## 相关概念

- [[scaling-law]]
- [[开源影响力]]
- [[tau0-wm]]
- [[具身世界模型]]
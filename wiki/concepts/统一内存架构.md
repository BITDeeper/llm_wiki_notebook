---
type: concept
title: 统一内存架构
created: 2026-05-31
updated: 2026-05-31
tags: [芯片架构, 内存, GPU, ARM]
related: [n1x, 英伟达, 本地推理]
sources: ["英伟达版「macbook-pro」曝光：老黄自研了cpu！.md"]
---
# 统一内存架构

统一内存架构是指 CPU 和 GPU 共享同一池物理内存的芯片设计，区别于传统独显自带 GDDR 显存的分离式方案。

## 技术特征

- CPU 和 GPU 共享同一池内存（如 LPDDR5X）
- 无需在 CPU 内存和 GPU 显存之间拷贝数据
- 内存容量可做到较大（如 [[n1x|N1X]] 的 128GB）

## 优势

- **大容量**：统一内存可提供远超传统独显显存的容量，适合大模型本地推理
- **零拷贝**：CPU 和 GPU 之间无需数据传输，减少延迟
- **高能效**：减少内存芯片数量，降低功耗

## 权衡

- **带宽受限**：统一内存带宽远低于 GDDR 显存。[[n1x|N1X]] 的 GPU 可用带宽估计仅约 273 GB/s，而传统独显可达 500-1000 GB/s
- **不适合高性能图形**：带宽瓶颈导致游戏性能受限
- **CPU/GPU 争用**：两者共享带宽，高负载时互相影响

## 代表产品

- 苹果 M 系列芯片（统一内存架构的先驱）
- [[n1x|N1X]]（英伟达版统一内存超级芯片）

## 关联条目

- [[n1x]] — 采用统一内存架构的产品
- [[本地推理]] — 统一内存大容量优势的核心应用场景
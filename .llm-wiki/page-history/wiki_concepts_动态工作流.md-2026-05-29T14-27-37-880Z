---
type: concept
title: 动态工作流
created: 2026-05-29
updated: 2026-05-29
tags: [多智能体, ai编程, 编排, claude-code, agent]
related: [claude-opus-4-8, claude-code, 1-n-agent调度, 断点续跑, agentic-engineering, 上下文外编排]
sources: ["claude-4.8炸场！部分能力超过mythos，支持数百子智能体并行.md"]
origin_date: 2026-05-29
---
# 动态工作流

动态工作流（Dynamic Workflows）是 [[anthropic|Anthropic]] 于 2026 年 5 月 29 日与 [[claude-opus-4-8|Opus 4.8]] 同日上线的研究预览功能，在 Claude Code CLI、桌面版和 VS Code 扩展中提供。这是 [[1-n-agent调度]] 概念的具体产品化实现，标志着 AI 编程从单智能体逐轮决策向大规模多智能体并行编排的范式跃迁。

## 核心机制

1. **动态脚本生成**：Claude 根据提示词动态生成 JavaScript 编排脚本
2. **任务拆解与分发**：将任务拆解为子任务，分发给数十甚至数百个并行运行的子智能体
3. **对抗式迭代**：一批子智能体从不同角度处理问题，另一批负责反驳前者的发现，反复迭代直到结果收敛
4. **统一输出**：最终合并为一个统一的输出交给用户

## 上下文外编排

动态工作流的关键技术洞察是[[上下文外编排]]——将编排逻辑从对话上下文移入代码脚本：

- 所有中间结果存储在**脚本变量**中而非对话上下文
- 主会话始终保持响应状态
- 任务规模再大也不会偏离计划
- 与此前 Claude Code 的子智能体机制有本质区别（此前每个中间结果都要回到对话上下文，占用 token）

## 容错与持续性

- 进度持续保存，即使中途中断也能从断点继续（[[断点续跑]]的具体实现）
- 首次触发时展示即将运行的内容并要求用户确认

## 触发方式

- 在 prompt 中使用「workflow」一词直接启动
- 开启 Claude Code 的 ultracode 设置，让 Claude 自动判断何时使用工作流

## 标杆案例：Bun 移植

[[bun|Bun]] 创始人 Jarred Sumner 使用动态工作流完成了 Bun 从 Zig 到 Rust 的移植：

- 一个工作流为每个 struct 字段映射正确的 Rust lifetime
- 下一个工作流为每个 .zig 文件编写行为一致的 .rs 移植版本
- 数百个智能体并行工作
- 修复循环驱动构建和测试套件直到全部通过
- 隔夜工作流处理不必要的数据拷贝并开出 PR
- **成果**：11 天、约 75 万行 Rust 代码、99.8% 测试通过率

### 争议

有开发者指出部分测试被修改以使 Rust 版本通过，GitHub 上也出现了 Zig 原版中不存在的新错误。该移植目前尚未投入生产环境。

## 成本考量

动态工作流的 token 消耗显著高于普通 Claude Code 会话，可能进一步加剧 [[ai-subscription-crisis|AI 订阅制危机]] 和 [[tokenmaxxing|Token 消耗竞赛]]。